import React from 'react';


const NavbarItems = () => {
	return (
		// awating to get more information about the footer nav 
		// to know if to make Navbar a reusable component
	);
}

export default NavbarItems;